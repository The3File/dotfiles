#!/usr/bin/env bash

op="$1"

main(){
    [[ -z $op ]] && restartbar
    [[ $op == "toggle" ]] && killbar
}

killbar(){
    [[ -z $(pgrep -x polybar) ]] && restartbar || killall -q polybar
}

restartbar(){
    killall -q polybar
    while pgrep -u $UID -x polybar >/dev/null; do sleep 1; done
    polybar dummy &
    polybar example &
}
main
