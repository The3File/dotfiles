#!/usr/bin/env bash

ask=$( echo -e "nevermind\nshutdown\nreboot\nexit i3" | dmenu -i -p "vil du udføre:" ) 

[[ $ask = "exit i3" ]] && i3-msg exit
[[ $ask = "shutdown" ]] && shutdown now
[[ $ask = "reboot" ]] && reboot
