#!/usr/bin/env bash

ask=$( echo -e "nevermind\nshutdown\nreboot\nexit i3" | dmenu -i -p "vil du udføre:" ) 

if [[ $ask = "exit i3" ]];then
	i3-msg exit
elif [[ $ask = "shutdown" ]];then
	shutdown now
elif [[ $ask = "reboot" ]];then
	reboot
fi
exit 0
