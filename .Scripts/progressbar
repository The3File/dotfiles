#!/usr/bin/env bash
# A try at something

stty -echo
elaps="0"
uI="$1"
size="(($(tput cols)-2))"
rows="$(tput lines)"

while ((elaps<uI))
do
	((elaps+=1))
	((total=elaps*size/uI))
	printf -v progr "%$((total))s"
	printf -v total "%$((size-total))s"
	printf '\e['$rows';0H%s\r' "[${progr// /\#}${total}]"
		sleep 1
done
printf "\nTest complete"
read &>/dev/null
